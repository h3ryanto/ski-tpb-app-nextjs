services:
  nextjs-app:
    container_name: nextjs-app
    build:
      context: .
      args:
        - RESEND_API_KEY=re_Rq6bLA4K_FxnGakKoT5WLJnS889Wpdo5J
    env_file:
      - .env
      - .env.local
    ports:
      - "3000:3000"
    restart: always
    volumes:
      - ski-app:/app/public
  volumes:
  ski-app:

   
